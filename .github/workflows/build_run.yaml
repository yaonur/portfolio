name: Portfolio site workflow
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on : ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: hey there
        run: echo "Hey there"
        shell: bash
  goodbye:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: bye bye
        run: echo "bye bye"
        shell: bash